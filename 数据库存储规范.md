# 空间多组学 `.h5mu` 最小存储规范

## 1. 总体要求

每个 `.h5mu` 文件必须满足：

- 至少包含 2 个生物组学模态；

- 每个模态包含数据矩阵、观测对象 ID 和特征 ID；

- 顶层包含空间坐标和样本 ID；

- 文件中能够明确解释矩阵数值、空间单位、坐标单位及模态配对关系。

组织图像、病理图像和细胞形态不单独计为组学模态。

---

## 2. 必须包含的结构

```yaml
MuData:
  mod:
    <modality_name>:
      X: required
      obs_names: required
      var_names: required
      uns:
        assay:
          technology: required
          value_type: required

  obs:
    index: required
    sample_id: required

  obsm:
    spatial: required

  uns:
    database:
      schema_version: required
      dataset_id: required
      source: required
      organism: required
      tissue: required
      spatial_unit: required
      coordinate_unit: required
      pairing_type: required
```

---

## 3. 模态命名

优先使用以下标准名称：

```text
rna
protein
atac
methylation
metabolite
lipid
```

不要使用不统一的名称，例如：

```text
RNA_data
gene_expression
ADT_matrix
protein_data
```

建议映射：

```text
gene_expression -> rna
transcriptomics -> rna
ADT -> protein
chromatin_accessibility -> atac
```

---

## 4. 每个模态的必需内容

### 4.1 `X`

位置：

```python
mdata.mod[modality].X
```

含义：

```text
行 = 细胞、细胞核、spot、bin 或 region
列 = 基因、蛋白、peak、代谢物等特征
```

要求：

```python
adata.X is not None
adata.X.shape == (adata.n_obs, adata.n_vars)
adata.n_obs > 0
adata.n_vars > 0
```

`X` 只保存一份主要数据矩阵，优先保存最接近原始测量值的数据。

---

### 4.2 `obs_names`

位置：

```python
mdata.mod[modality].obs_names
```

要求：

- 不为空；

- 在模态内部唯一；

- 使用稳定 ID；

- 同一生物学对象在不同模态中使用相同 ID；

- 必须属于顶层 `mdata.obs_names`。

推荐格式：

```text
sample01_cell_000001
sample01_cell_000002
```

不同模态可以只包含部分观测对象，不要求所有模态拥有完全相同的细胞集合。

---

### 4.3 `var_names`

位置：

```python
mdata.mod[modality].var_names
```

要求：

- 不为空；

- 在模态内部唯一；

- 能唯一识别对应特征。

推荐格式：

```text
RNA: Ensembl ID 或 gene symbol
Protein: protein/antibody target name
ATAC: chr:start-end
Methylation: CpG ID 或 genomic interval
Metabolite: HMDB ID 或标准化名称
```

---

### 4.4 `uns["assay"]`

位置：

```python
mdata.mod[modality].uns["assay"]
```

格式：

```python
{
    "technology": "Xenium",
    "value_type": "counts"
}
```

字段定义：

```text
technology = 数据产生技术
value_type = X 矩阵数值的含义
```

推荐 `value_type`：

```text
counts
binary
intensity
normalized
log_normalized
background_corrected_intensity
accessibility_score
unknown
```

除非无法确定，否则不要使用 `unknown`。

---

## 5. 顶层观测信息

### 5.1 `mdata.obs_names`

表示文件内全部观测对象的统一 ID 集合。

要求：

```python
mdata.obs_names.is_unique
```

每个模态的 `obs_names` 必须是该集合的子集。

---

### 5.2 `mdata.obs["sample_id"]`

位置：

```python
mdata.obs["sample_id"]
```

含义：

```text
每个观测对象所属的样本、组织切片或独立空间坐标系
```

即使文件只有一个样本，也必须填写：

```text
sample_01
```

要求：

```python
"sample_id" in mdata.obs.columns
mdata.obs["sample_id"].notna().all()
```

不同 `sample_id` 的空间坐标默认属于不同的局部坐标系。

---

## 6. 空间坐标

位置：

```python
mdata.obsm["spatial"]
```

格式：

```text
二维数据：N × 2，列顺序为 [x, y]
三维数据：N × 3，列顺序为 [x, y, z]
```

要求：

```python
"spatial" in mdata.obsm
mdata.obsm["spatial"].shape[0] == mdata.n_obs
mdata.obsm["spatial"].shape[1] in (2, 3)
```

推荐使用数值类型：

```text
float32
```

---

## 7. 数据集级元数据

位置：

```python
mdata.uns["database"]
```

格式：

```python
mdata.uns["database"] = {
    "schema_version": "1.0",
    "dataset_id": "GSE277793_xenium",
    "source": "GSE277793",
    "organism": "Homo sapiens",
    "tissue": "kidney",
    "spatial_unit": "cell",
    "coordinate_unit": "micrometer",
    "pairing_type": "same_unit"
}
```

字段定义：

```text
schema_version:
    数据库内部格式版本。

dataset_id:
    数据库内唯一的数据集 ID。

source:
    数据来源标识，例如 GEO、Zenodo、DOI 或官方数据集编号。

organism:
    物种。

tissue:
    主要组织。

spatial_unit:
    矩阵每一行代表的空间单位。

coordinate_unit:
    空间坐标的单位。

pairing_type:
    不同模态之间的观测对象对应关系。
```

推荐枚举：

```text
spatial_unit:
    cell
    nucleus
    spot
    bin
    region

coordinate_unit:
    micrometer
    millimeter
    pixel

pairing_type:
    same_unit
    partially_shared
    unpaired
```

配对关系定义：

```text
same_unit:
    不同模态测量相同的一组观测对象。

partially_shared:
    不同模态只有部分观测对象重叠。

unpaired:
    不同模态来自不同细胞、不同区域或相邻切片。
```

---

## 8. 最小验证函数

```python
def validate_minimal_h5mu(mdata):
    required_database_fields = {
        "schema_version",
        "dataset_id",
        "source",
        "organism",
        "tissue",
        "spatial_unit",
        "coordinate_unit",
        "pairing_type",
    }

    # 至少两个组学模态
    assert len(mdata.mod) >= 2

    # 顶层观测对象
    assert mdata.n_obs > 0
    assert mdata.obs_names.is_unique
    assert "sample_id" in mdata.obs.columns
    assert mdata.obs["sample_id"].notna().all()

    # 空间坐标
    assert "spatial" in mdata.obsm
    spatial = mdata.obsm["spatial"]
    assert spatial.shape[0] == mdata.n_obs
    assert spatial.shape[1] in (2, 3)

    # 数据集级元数据
    assert "database" in mdata.uns
    assert required_database_fields.issubset(
        mdata.uns["database"].keys()
    )

    global_obs_names = set(mdata.obs_names)

    # 各模态验证
    for modality, adata in mdata.mod.items():
        assert adata.X is not None
        assert adata.n_obs > 0
        assert adata.n_vars > 0

        assert adata.obs_names.is_unique
        assert adata.var_names.is_unique

        assert set(adata.obs_names).issubset(global_obs_names)

        assert "assay" in adata.uns
        assert "technology" in adata.uns["assay"]
        assert "value_type" in adata.uns["assay"]

    return True
```

---

## 9. 不强制保存的内容

以下内容全部为可选，不应影响文件是否通过最小验证：

```text
layers
raw
PCA
UMAP
聚类结果
细胞类型注释
QC 指标
归一化矩阵
高变基因
空间邻接图
组织图像
细胞分割边界
细胞核边界
转录本级坐标
配体受体分析结果
完整分析流程
```

---

## 10. 最小必需信息总结

一个合格的最小 `.h5mu` 文件必须能够回答：

```text
1. 有哪些组学模态？
2. 每个模态的数据矩阵是什么？
3. 每一行对应哪个空间观测对象？
4. 每一列对应哪个生物学特征？
5. 每个观测对象属于哪个样本？
6. 每个观测对象的空间坐标是什么？
7. 矩阵中的数值代表什么？
8. 数据由什么技术产生？
9. 数据来自哪里？
10. 空间单位和坐标单位是什么？
11. 不同模态是否测量同一批观测对象？
```

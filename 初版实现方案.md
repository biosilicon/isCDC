# 空间多组学科研数据库初版方案概述

初版采用轻量级、公开只读的设计，核心目标是管理符合统一存储规范的 `.h5mu` 文件，并通过网页提供数据集检索、信息浏览和完整文件下载。

## 技术栈

- 后端：FastAPI

- 网页模板：Jinja2

- 页面样式：Bootstrap

- 数据库：SQLite

- ORM：SQLAlchemy

- `.h5mu` 读取与验证：MuData、AnnData、NumPy、SciPy

- 文件存储：服务器本地文件系统

- 文件下载：Nginx

- 部署：Docker Compose

初版不采用独立前端框架、对象存储、任务队列或矩阵分块访问，以降低开发和维护成本。

## 主要实现思路

每个数据集由两个文件组成：

```text
dataset.h5mu
metadata.yaml
```

其中：

- `.h5mu` 保存完整空间多组学数据；

- `metadata.yaml` 由维护者手动填写，用于网页展示和数据集筛选。

数据通过命令行工具导入。导入程序依次完成：

1. 验证人工元数据格式；

2. 验证 `.h5mu` 是否符合数据库存储规范；

3. 检查人工元数据与文件内部关键字段是否一致；

4. 计算文件大小和 SHA-256 校验值；

5. 生成验证报告和数据清单；

6. 将文件保存到正式目录；

7. 将网页所需元数据写入 SQLite。

网站只读取 SQLite 中的目录信息，不在用户访问网页时读取大型 `.h5mu` 文件。

## 网页功能

初版网页主要包括：

- 数据库首页；

- 数据集列表；

- 关键词搜索；

- 按物种、组织、模态、技术和空间单位筛选；

- 数据集详情页；

- `.h5mu`、元数据和验证报告下载；

- 简单的公开 API。

网站不提供在线表达矩阵查询、基因表达可视化、数据子集生成或大矩阵分块访问。

## 关键注意点

### 数据完整性验证

导入时必须验证：

- 至少包含两个组学模态；

- 每个模态具有有效的 `X`、`obs_names` 和 `var_names`；

- 顶层具有 `sample_id` 和空间坐标；

- 数据集级和模态级必需元数据完整；

- 不同模态的观测对象关系符合 `pairing_type`；

- ID 唯一，矩阵维度和坐标维度正确。

大型矩阵只进行结构检查，不做逐元素扫描，避免导入过程占用过多内存和时间。

### 人工元数据一致性

虽然网页元数据手动提供，但以下字段应与 `.h5mu` 内部严格一致：

```text
dataset_id
modalities
spatial_unit
coordinate_unit
pairing_type
```

标题、简介、关键词、许可证和文献信息可以完全由人工维护。

### 文件与数据库分离

SQLite 只保存数据目录和展示信息，不保存表达矩阵。完整数据始终保存在 `.h5mu` 文件中。

正式目录建议同时保存：

```text
dataset.h5mu
metadata.yaml
manifest.json
validation_report.json
checksum.sha256
```

这样即使 SQLite 损坏，也可以根据这些文件重建数据库索引。

### 原子导入

导入过程中应先使用临时目录。只有所有验证和文件操作均成功后，才写入正式目录和 SQLite，避免产生不完整数据集。

### 下载方式

开发环境可以由 FastAPI 返回文件；正式部署建议由 Nginx 负责传输大型 `.h5mu` 文件，减少后端进程压力。

## 总体架构

```text
dataset.h5mu + metadata.yaml
              ↓
        命令行导入与验证
              ↓
      SQLite 元数据目录
       + 本地文件存储
              ↓
      FastAPI + Jinja2
              ↓
        公开浏览与下载
```

该方案保留了科研数据库所需的数据规范性、完整性、可追溯性和可下载性，同时将初版系统复杂度控制在较低水平，后续可根据实际需求再扩展用户系统、在线可视化或矩阵访问功能。

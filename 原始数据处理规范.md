# 原始数据处理工作流

本流程用于处理 `temp/` 目录下彼此独立的数据集。流程包含
`dataset_planner` 和 `dataset_worker` 两种 subagent 角色，但完整处理分为五个阶段。
角色定义与并发配置分别位于 `.codex/agents/` 和 `.codex/config.toml`；
`.codex/` 为不纳入版本控制的本地配置，执行前必须确认所需角色可用。

每个数据集应位于独立的 `temp/<dataset_name>/` 目录中。原始输入在整个流程中
保持只读；中间文件和待验收产物分别写入该数据集内的独立目录。
所有最终产物必须符合 [数据库存储规范 1.2](数据库存储规范_v1.2.md)。
WSI 等非组学文件必须在规划时区分为辅助文件：它们不写入 `.h5mu`、不计为模态，并且只能
在所属主数据成功入库后由主 agent 串行注册。

## 第一阶段：并行规划

1. 扫描 temp/ 下的数据集目录。
2. 每个数据集启动一个 dataset_planner。
3. 每个 planner 只能负责一个数据集。
4. 最多同时运行 3 个 planner。
5. planner 必须保持只读，只调查数据并制定计划。
   若目录或可靠来源包含候选 WSI 等辅助文件，还必须核对其与具体样本或切片的一一对应关系、
   原始来源 URL、文件格式、许可信息以及是否确有保存和公开下载价值。
   同时检查原始公开来源是否提供离散 cell type 标签；只有标签可与全部顶层 observation
   一一对齐时才列为候选。论文图中的聚类或 label transfer、空间区域名称以及仅覆盖部分
   observation 的注释不得直接判定为可入库 cell type。
6. 等待所有 planner 返回后再继续。
7. 汇总各 planner 的结果，并检查其结论是否与数据库规范一致。
8. 将决策划分为：
   - 全局数据库决策；
   - 多数据集共享决策；
   - 单数据集特定决策。
9. 合并重复问题，避免让我对同一规则重复作答。
10. 对存在冲突的 planner 建议进行分析，不得机械接受其推荐。
11. 一次性向我展示：
    - 各数据集摘要；
    - 推荐处理方案；
    - 所有需要决定的问题；
    - 各选项的影响；
    - 主 agent 的客观建议。
12. 在我回答前，不得启动 dataset_worker。

## 第二阶段：计划定稿

收到我的选择后：

1. 将全局决定传播给所有受影响的数据集。
2. 为每个数据集生成最终执行计划。
3. 最终计划必须明确：
   - 输入文件；
   - 输出路径；
   - observation 和 feature 处理规则；
   - 模态对齐规则；
   - 空间坐标处理规则；
   - 元数据映射；
   - 可选 `cell_type` 的来源文件、来源字段、对齐键、覆盖率、分类层级和 category 顺序；若不
     保存该列，明确记录是来源未提供、无法可靠对齐、语义不是 cell type，还是覆盖不完整；
   - 条件性辅助文件的稳定 ID、原始文件名、来源 URL、MIME 类型和所属数据集；
   - 辅助文件的下载/生成方式、格式检查、预期大小或上游 checksum（若来源提供）；
   - 验证标准；
   - 失败回退方式。
4. 向我展示最终计划。
5. 只有我明确批准后才能进入执行阶段。

## 第三阶段：并行执行

1. 每个获批数据集启动一个 dataset_worker。
2. 每个 worker 只能负责一个数据集。
3. 最多同时运行 5 个 worker；若当前运行平台的 agent 总并发上限更低，
   或可用内存、临时磁盘和磁盘 I/O 不足，则以较低的安全并发数为准。
4. 将对应的最终计划和用户决定完整传递给 worker。
5. 为每个数据集分配独立工作目录，例如：
   `temp/<dataset_name>/.work/`。
6. 为每个数据集分配独立输出目录，例如：
   `temp/<dataset_name>/processed/`。
7. worker 不得直接写入共享数据库。
8. worker 如需下载或生成获批辅助文件，只能写入本数据集专属工作/输出目录；不得直接写入
   `data/datasets/`、修改正式 manifest 或调用辅助文件注册 CLI。
9. worker 不得删除 temp/ 下的原始数据。
10. worker 只能使用批准计划指定的来源标签，不得根据表达矩阵、聚类结果或名称相似性自行
    推断 `cell_type`。存在时统一写入顶层 `mdata.obs["cell_type"]`，格式为完整覆盖、无序、
    无空值、无首尾空白且无未使用 category 的 pandas categorical。
11. 等待所有 worker 完成后再进入主 agent 验收。

这里禁止的是在原始数据整理/入库阶段把模型结果冒充来源 `cell_type`。已导入 Database 的
离线 SingleR/RCTD 可视化属于另一条隔离流程，只能写入忽略的
`data/cell_type_visualizations/` sidecar，绝不写回 worker 输出或正式 `.h5mu`。相关环境、QC、
失败撤下和 provenance 要求见 [实施与完成记录](plan.md) 与
[注释经验总结](annotation/细胞类型注释经验总结.md)。

## 第四阶段：主 agent 验收

对每个 worker 的结果进行独立复核：

1. 重新打开输出文件。
2. 核对数据库必填字段。
3. 核对模态、shape、observation ID 和 feature。
4. 核对空间坐标。
5. 若产物包含 `cell_type`，逐 observation 回查批准的来源标签，检查完整覆盖、categorical
   dtype、无序属性、字符串 category、空值/空白和未使用 category；若计划要求省略，也确认
   没有残留同名列。不得仅比较类别数或 value counts 代替逐行对齐。
6. 核对验证报告与实际文件是否一致。
7. 对每个候选辅助文件核对：与样本/切片的对应关系、常规文件状态、格式/首目录可读性、
   字节数、SHA-256、来源 URL、MIME 类型，以及批准计划要求的格式特异元数据。
8. 检查 worker 是否偏离批准计划。
9. 主数据或其计划内辅助文件验证失败时不得入库或注册对应失败产物。
10. 将失败原因和可恢复步骤报告给我。

## 第五阶段：串行入库与清理

1. 仅对验收通过的数据集执行数据库导入。
2. 数据库导入必须由主 agent 串行完成，不得由多个 worker 并发写入。
   新数据使用普通 `import-dataset`；更新已有 ID 必须显式使用 `import-dataset --replace`。
   替换必须保持原 `dataset_type`，衍生数据还必须保持 construction type、按顺序排列的
   source IDs、`split_id` 和 `challenge_type`。命令负责暂存验证、保留已注册辅助文件及失败
   回滚，不得以手工覆盖正式目录或直接改 SQLite 代替。
3. 主数据导入成功后，主 agent 才能使用 `add-auxiliary-file` 逐个串行注册获批辅助文件；不得
   手工复制正式文件或直接编辑 manifest。
4. 每次主数据导入后重新读取数据库记录和对应数据文件；每次辅助文件注册后重新核对 CLI
   结果、正式副本大小/SHA-256、manifest 1.1、格式以及详情页/API/HEAD/Range 下载。
   若注册 ID 为 `he_wsi`，还必须由主 agent 执行
   `generate-wsi-thumbnails <dataset_id> [--force]`，确认输出是直接由正式 WSI 生成的完整
   切片视图、纵横比正确、最长边 640 px，且无瓦片断裂、黑边或方向错误。
   全部注册和缩略图生成完成后记录待发布变更；若本批还修改了 Challenge，则等第 6 步
   difficulty 快照生成后统一重启并验证，避免加载中间状态。仅处理 Database/辅助文件时可在
   此处重启，并直接验证所属详情页和静态 WebP 响应。
5. 注册、缩略图生成或验证任一辅助文件失败时，保留其 temp 来源和已成功入库的主数据，报告可恢复步骤；
   不得用不完整或未登记的正式文件替代失败注册。
6. 完成一批 train/test 导入、替换或移除后，统一运行
   `PYTHONPATH=src python -m iscdc.cli evaluate-challenge-difficulty --force`。检查报告中的
   `challenge_count` 与当前目录一致，逐项失败已明确记录，成功项的 train/test ID、SHA-256、
   指标范围和排序一致；CLI 因局部失败返回非零时不得丢弃已原子生成的报告，应先核对失败项并
   决定修复后重算或以 unavailable 发布。随后重启应用，直接检查 Challenge 目录/详情页、
   `difficulty_asc`/`difficulty_desc` 排序和 JSON API。不要在每导入一个文件后发布临时
   percentile；相对排名必须基于最终整批 catalogue。
7. 确认数据库记录、文件路径、数据集标识、主文件、全部计划内辅助文件以及 difficulty 快照
   均正常。仅导入 `full` Database 且 Challenge 集合和文件完全未变时，不需要重算 difficulty。
   若更新的 full 被现有 Challenge 引用，必须判断 `cell_type` 等 observation 级可选列是否需要
   传播；需要时先从更新后的 full 重新生成并替换所有受影响 train/test，再执行第 6 步。
8. 只有满足以下全部条件后，才能删除对应的 temp/ 数据集目录：
   - 转换成功；
   - worker 验证通过；
   - 主 agent 复核通过；
   - 数据库导入成功；
   - 导入后检查通过。
   - 所有计划内辅助文件均已成功注册并通过独立复核，所有 `he_wsi` 已生成并验收网页
     缩略图，或批准计划明确声明没有辅助文件。
9. 任一条件不满足时保留原始数据，不得删除。
10. 删除操作前列出准备删除的精确路径，禁止使用模糊通配符。
11. 最后汇总主数据、辅助文件、difficulty 评估的成功/失败项以及原始数据的保留和删除情况。

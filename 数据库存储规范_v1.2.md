# isCDC Schema 1.2：跨组学翻译 `.h5mu` 存储规范

## 1. 适用范围与数据集类型

本数据库面向跨组学翻译任务，只收录至少存在一组可配对模态的空间多组学数据。
数据库中的 `.h5mu` 文件分为三种数据集类型：

```text
full   全集
train  训练集
test   测试集
```

三者关系如下：

- **全集（`full`）**是数据库中最基本、最原始的存储单位；
- 本规范对组学矩阵、观测对象、空间坐标和数据来源等通用结构的规定，首先适用于全集；
- **训练集（`train`）和测试集（`test`）均为衍生数据集，只能由一个或多个全集产生；**
- 训练集和测试集不得由另一个训练集或测试集继续衍生；
- 每个训练集和测试集都必须保留到来源全集及来源观测对象的可追溯关系。

训练集和测试集有两种构成方式：

```text
subset:
    来自一个全集的子集。

composite:
    由多个全集共同组成；每个来源全集可以贡献全部或部分观测对象。
```

`train` 和 `test` 表示数据在某一数据划分中的用途，不表示其数据内容或实验技术发生变化。

### 1.1 与网页目录类别的映射

网页和目录 API 在不增加新的 `dataset_type` 值或单独的目录类别字段的情况下，将上述
存储类型映射为两个读取类别：

```text
Database:
    dataset_type == "full" 的单个文件。

Challenge:
    以 derivation.split_id 为标识的一次数据划分，包含对应的 train 和 test 文件。
```

Database/Challenge 两类的映射仅用于目录呈现，不改变 `dataset_type`。Challenge 的三种
评估边界则按下述要求存储在 `derivation.challenge_type` 中，不应另建目录分类字段。
相同 `split_id` 的 train/test 在网页上只呈现为一个 Challenge；若某一侧尚未导入，目录会
将其标记为不完整，但不会伪造文件。同一 `split_id` 下出现多份 train 或多份 test 时，
目录将其视为完整性错误，不会静默选择其中一份。

每个 Challenge 还必须通过两侧一致的 `derivation.challenge_type` 声明评估边界：

```text
same_slice                 同切片
cross_slice_same_subject   同个体跨切片
cross_subject              跨个体（含生物学重复、不同条件或不同阶段）
```

---

## 2. 总体要求

每个 `.h5mu` 文件必须满足：

- 至少包含 2 个生物组学模态；
- 每个模态包含数据矩阵、观测对象 ID 和特征 ID；
- 顶层包含空间坐标和样本 ID；
- 文件中能够明确解释矩阵数值、空间单位、坐标单位及模态配对关系；
- 恰好两个模态时，两侧必须覆盖完全相同的观测对象；
- 三个及以上模态时允许部分共享，但至少一对模态必须存在共同观测对象；
- 任意模态之间均无共同观测对象的 `unpaired` 文件不得入库；
- 顶层元数据必须明确标记数据集类型；
- 训练集和测试集必须额外记录衍生方式、Challenge 类型、来源全集和来源观测对象。

组织图像、病理图像和细胞形态不单独计为组学模态。

---

## 3. 必须包含的结构

### 3.1 通用结构

所有全集、训练集和测试集均必须包含：

```yaml
MuData:
  mod:
    <modality_name>:
      X: required
      obs_names: required
      var_names: required
      uns:
        assay:
          technology: required
          value_type: required

  obs:
    index: required
    sample_id: required

  obsm:
    spatial: required

  uns:
    database:
      schema_version: required
      dataset_id: required
      dataset_type: required
      source: required
      organism: required
      tissue: required
      spatial_unit: required
      coordinate_unit: required
      pairing_type: required
```

### 3.2 训练集和测试集的附加结构

当 `dataset_type` 为 `train` 或 `test` 时，还必须包含：

```yaml
MuData:
  obs:
    source_dataset_id: required
    source_obs_id: required

  uns:
    database:
      derivation:
        construction_type: required
        source_dataset_ids: required
        split_id: required
        challenge_type: required
        selection_description: required
        feature_merge_policy: required
        processing_description: required
        reference_dataset_id: conditionally_required
        random_seed: optional
```

其中 `reference_dataset_id` 仅在 `feature_merge_policy == "reference"` 时为必需字段。

### 3.3 辅助文件

组织切片 WSI 等辅助文件不写入 `.h5mu`、不计为组学模态，也不增加 catalogue 字段或新的
`dataset_type`。辅助文件必须属于一个已经入库的数据文件，并放在该数据目录的
`auxiliary/` 子目录。不得把正式辅助下载文件放在网页静态资源或缩略图采集目录中，也不得为
它创建独立 catalogue 记录。

新导入的数据集使用 manifest 1.1，并包含允许为空的顶层 `auxiliary_files` 列表。旧版
manifest 1.0 没有该列表，读取时等价于无辅助文件。注册辅助文件时应将 manifest 原子升级为
1.1，不改变 `.h5mu` 的 `schema_version`。每个辅助项必须具有以下结构：

```json
{
  "id": "he_wsi",
  "name": "auxiliary/example.ome.tif",
  "label": "H&E whole-slide image (OME-TIFF)",
  "media_type": "image/tiff",
  "size": 1427110955,
  "sha256": "<64 lowercase hexadecimal characters>",
  "source_url": "https://example.org/example.ome.tif",
  "retrieved_at": "2026-08-12T05:30:02.792907+00:00"
}
```

字段约束如下：

- `id` 在所属数据文件内唯一，长度为 1–64，只允许小写 ASCII 字母、数字、下划线和连字符，
  且首字符必须是字母或数字；它是下载路由使用的稳定标识，不是文件路径。
- `name` 在清单内唯一，必须是 `auxiliary/<原始文件名>` 形式的单层受控相对路径；不得包含
  绝对路径、反斜杠、`.`、`..`、额外子目录或符号链接。
- `label` 是非空显示名称，`media_type` 是明确的 MIME 类型；两者长度均不得超过 200 字符。
- `size` 是非负字节数，必须与正式文件一致；`sha256` 必须是正式文件的 64 位小写十六进制
  SHA-256。
- `source_url` 必须是绝对 HTTP(S) 原始来源，`retrieved_at` 必须是带时区的 ISO 8601 时间。

辅助文件只能在所属主数据成功入库后由串行注册流程添加。注册必须拒绝未知数据集、符号链接、
路径穿越、重复 ID、重复目标名和覆盖；复制过程中计算大小与 SHA-256，并在文件就位后原子
替换 manifest。若 manifest 更新失败，必须移除新复制的文件并保留原 manifest。主
`checksum.sha256` 和 `validation_report.json` 继续只描述 `.h5mu`；辅助文件完整性由 manifest
中的 `size` 和 `sha256` 负责。

辅助文件不得拥有独立目录条目或详情页。网站可在所属数据详情页提供下载；服务端必须根据
manifest 中的稳定 ID 查找文件，不得接受用户提供的原始路径。JSON 数据文件记录使用
`auxiliary_files` 列表公开 `id`、显示和校验字段、原始来源及本站下载 URL。下载端点必须支持
HEAD 和 HTTP byte Range，以便大型 WSI 断点续传。某一数据文件的辅助清单无效、文件缺失或
大小不符时，应隐藏该数据文件的整份辅助清单并记录警告；不得影响主目录、JSON API 或主数据
文件下载。新增、删除或注册辅助文件后必须重启应用以重建启动时索引。

对 `dataset_type == "full"` 且注册了稳定 ID `he_wsi` 的 Database，网页缩略图必须直接由该
正式 WSI 副本通过 `generate-wsi-thumbnails` 生成，不得使用另一张平台预览图或论文图替代。
输出固定为本地 `assets/static/database_thumbnails/<dataset_id>.webp`，不进入 catalogue 或
manifest，也不纳入 Git。
生成时应保留整张切片、TIFF orientation 和原始纵横比，不裁剪组织区域；有金字塔时读取
最小且最长边不低于 640 px 的层，无金字塔时读取完整图像，再以 Lanczos 降采样为
最长边 640 px 的 RGB WebP，编码参数固定为 `quality=85`、`method=6`。已有输出默认不得覆盖；
显式覆盖时必须先在同目录生成并校验临时 WebP，然后原子替换。新增或替换缩略图后必须
重启应用。

---

## 4. 模态命名

优先使用以下标准名称：

```text
rna
protein
atac
histone
methylation
metabolite
lipid
```

不要使用不统一的名称，例如：

```text
RNA_data
gene_expression
ADT_matrix
protein_data
```

建议映射：

```text
gene_expression -> rna
transcriptomics -> rna
ADT -> protein
chromatin_accessibility -> atac
H3K27ac -> histone
H3K27me3 -> histone
H3K4me3 -> histone
```

组蛋白修饰数据使用通用模态名 `histone`，不将具体标记名称（如
`h3k27ac`、`h3k27me3` 或 `h3k4me3`）作为模态名。具体标记应在
`mdata.uns["database"]["histone_mark"]` 中记录，并在该模态的
`technology` 中明确说明。例如：

```python
mdata.mod["histone"].uns["assay"] = {
    "technology": "Spatial CUT&Tag-RNA-seq (H3K27me3)",
    "value_type": "counts",
}
mdata.uns["database"]["histone_mark"] = "H3K27me3"
```

由于一个 MuData 文件中的模态名不能重复，schema 1.2 的单个文件
只表示一种组蛋白标记。同一样本若有多种组蛋白标记，应保存为多个
独立的 `full` 数据集。

---

## 5. 每个模态的必需内容

### 5.1 `X`

位置：

```python
mdata.mod[modality].X
```

含义：

```text
行 = 细胞、细胞核、spot、bin 或 region
列 = 基因、蛋白、peak、代谢物等特征
```

要求：

```python
adata.X is not None
adata.X.shape == (adata.n_obs, adata.n_vars)
adata.n_obs > 0
adata.n_vars > 0
```

`X` 只保存一份主要数据矩阵，优先保存最接近原始测量值的数据。

训练集和测试集不得在没有记录的情况下改变来源全集中的矩阵数值。任何归一化、对数转换、批次校正、特征缩放或其他处理，都必须在 `processing_description` 中说明，并通过各模态的 `value_type` 准确描述处理后的数值含义。

---

### 5.2 `obs_names`

位置：

```python
mdata.mod[modality].obs_names
```

要求：

- 不为空；
- 在模态内部唯一；
- 使用稳定 ID；
- 同一生物学对象在不同模态中使用相同 ID；
- 必须属于顶层 `mdata.obs_names`。

推荐格式：

```text
sample01_cell_000001
sample01_cell_000002
```

不同模态可以只包含部分观测对象，不要求所有模态拥有完全相同的细胞集合。

对于由多个全集组成的训练集或测试集，若不同来源全集存在重复的观测对象 ID，必须重新生成全局唯一的顶层 `obs_names`。推荐格式：

```text
<source_dataset_id>::<source_obs_id>
```

原始观测对象 ID 必须完整保存在 `mdata.obs["source_obs_id"]` 中。

---

### 5.3 `var_names`

位置：

```python
mdata.mod[modality].var_names
```

要求：

- 不为空；
- 在模态内部唯一；
- 能唯一识别对应特征。

推荐格式：

```text
RNA: Ensembl ID 或 gene symbol
Protein: protein/antibody target name
ATAC: chr:start-end
Histone: chr:start-end
Methylation: CpG ID 或 genomic interval
Metabolite: HMDB ID 或标准化名称
```

训练集和测试集进行特征合并时，必须按照 `feature_merge_policy` 对同一模态的特征空间进行显式对齐，不得仅依赖列位置进行拼接。

---

### 5.4 `uns["assay"]`

位置：

```python
mdata.mod[modality].uns["assay"]
```

格式：

```python
{
    "technology": "Xenium",
    "value_type": "counts"
}
```

字段定义：

```text
technology = 数据产生技术
value_type = X 矩阵数值的含义
```

推荐 `value_type`：

```text
counts
binary
intensity
normalized
log_normalized
background_corrected_intensity
accessibility_score
unknown
```

除非无法确定，否则不要使用 `unknown`。

若训练集或测试集由采用不同技术的多个全集组成，`technology` 可以保存为去重后的字符串列表；若某个模态在所有来源全集中使用相同技术，仍应保存为单个字符串。

---

## 6. 顶层观测信息

### 6.1 `mdata.obs_names`

表示文件内全部观测对象的统一 ID 集合。

要求：

```python
mdata.obs_names.is_unique
```

每个模态的 `obs_names` 必须是该集合的子集。

---

### 6.2 `mdata.obs["sample_id"]`

位置：

```python
mdata.obs["sample_id"]
```

含义：

```text
每个观测对象所属的样本、组织切片或独立空间坐标系
```

即使文件只有一个样本，也必须填写：

```text
sample_01
```

要求：

```python
"sample_id" in mdata.obs.columns
mdata.obs["sample_id"].notna().all()
```

不同 `sample_id` 的空间坐标默认属于不同的局部坐标系。

由多个全集组成训练集或测试集时，`sample_id` 在衍生文件中必须保持可区分。若不同来源全集存在重复的 `sample_id`，应使用以下形式重新编码：

```text
<source_dataset_id>::<original_sample_id>
```

---

### 6.3 `mdata.obs["source_dataset_id"]`

仅训练集和测试集必需。

含义：

```text
当前观测对象直接来源的全集 dataset_id
```

要求：

```python
"source_dataset_id" in mdata.obs.columns
mdata.obs["source_dataset_id"].notna().all()
```

该列中的每个值都必须出现在：

```python
mdata.uns["database"]["derivation"]["source_dataset_ids"]
```

`source_dataset_id` 必须指向 `dataset_type == "full"` 的数据集，不能指向训练集或测试集。

---

### 6.4 `mdata.obs["source_obs_id"]`

仅训练集和测试集必需。

含义：

```text
观测对象在来源全集中的原始 obs_name
```

要求：

```python
"source_obs_id" in mdata.obs.columns
mdata.obs["source_obs_id"].notna().all()
```

以下二元组在同一个衍生文件中必须唯一：

```text
(source_dataset_id, source_obs_id)
```

该二元组是跨训练集、测试集和全集检查观测对象来源及数据泄漏的主要标识。

---

## 7. 空间坐标

位置：

```python
mdata.obsm["spatial"]
```

格式：

```text
二维数据：N × 2，列顺序为 [x, y]
三维数据：N × 3，列顺序为 [x, y, z]
```

要求：

```python
"spatial" in mdata.obsm
mdata.obsm["spatial"].shape[0] == mdata.n_obs
mdata.obsm["spatial"].shape[1] in (2, 3)
```

推荐使用数值类型：

```text
float32
```

训练集和测试集应保留来源全集中的原始空间坐标和坐标单位。多个来源全集的坐标不要求被变换到同一个全局坐标系；不同 `sample_id` 的坐标仍视为各自独立的局部坐标系。

如对空间坐标进行了平移、缩放、旋转、镜像或配准，必须在 `processing_description` 中记录。

---

## 8. 数据集级元数据

位置：

```python
mdata.uns["database"]
```

### 8.1 全集示例

```python
mdata.uns["database"] = {
    "schema_version": "1.2",
    "dataset_id": "GSE277793_xenium",
    "dataset_type": "full",
    "source": "GSE277793",
    "organism": "Homo sapiens",
    "tissue": "kidney",
    "spatial_unit": "cell",
    "coordinate_unit": "micrometer",
    "pairing_type": "same_unit"
}
```

### 8.2 训练集或测试集示例

```python
mdata.uns["database"] = {
    "schema_version": "1.2",
    "dataset_id": "kidney_train_v1",
    "dataset_type": "train",
    "source": ["GSE277793", "ZENODO_123456"],
    "organism": "Homo sapiens",
    "tissue": ["kidney", "renal tumor"],
    "spatial_unit": "cell",
    "coordinate_unit": "micrometer",
    "pairing_type": "same_unit",
    "derivation": {
        "construction_type": "composite",
        "source_dataset_ids": [
            "GSE277793_xenium",
            "zenodo123456_xenium"
        ],
        "split_id": "kidney_benchmark_v1",
        "challenge_type": "cross_subject",
        "selection_description": (
            "使用两个全集中通过质量控制的全部观测对象"
        ),
        "feature_merge_policy": "intersection",
        "processing_description": (
            "按共同特征对齐；X 数值保持不变"
        ),
        "random_seed": None
    }
}
```

### 8.3 通用字段定义

```text
schema_version:
    数据库内部格式版本。本修订版使用 1.2。

dataset_id:
    数据库内唯一的数据集 ID。全集、训练集和测试集均不得重复。

dataset_type:
    数据集类型，只能为 full、train 或 test。

source:
    原始数据来源标识，例如 GEO、Zenodo、DOI 或官方数据集编号。
    全集通常为字符串；由多个全集组成的训练集或测试集可以为字符串列表。

organism:
    物种。全集通常为字符串；多全集衍生数据中存在多个物种时，保存去重后的字符串列表。

tissue:
    主要组织。全集通常为字符串；多全集衍生数据中存在多个组织时，保存去重后的字符串列表。

spatial_unit:
    矩阵每一行代表的空间单位。

coordinate_unit:
    空间坐标的单位。

pairing_type:
    不同模态之间的观测对象对应关系。

derivation:
    训练集和测试集的衍生信息。全集不需要该字段。

challenge_type:
    Challenge 的评估边界，位于 derivation 中，只能为 same_slice、
    cross_slice_same_subject 或 cross_subject。同一 split_id 的两侧必须一致。
```

推荐枚举：

```text
dataset_type:
    full
    train
    test

spatial_unit:
    cell
    nucleus
    spot
    bin
    region

coordinate_unit:
    micrometer
    millimeter
    pixel
    array_index

pairing_type:
    same_unit
    partially_shared
```

`array_index` 表示实验平台原生的无物理单位离散阵列索引。例如对使用 10x Visium
`array_row` 和 `array_col` 的数据，可在 `obsm["spatial"]` 中按 `[x, y] =
[array_col, array_row]` 保存并声明 `coordinate_unit: array_index`。未进行明确坐标
换算时，不应将这类索引标记为 `pixel`、`micrometer` 或其他物理单位。

配对关系定义：

```text
same_unit:
    不同模态测量相同的一组观测对象。

partially_shared:
    三个及以上模态的数据中，不同模态只有部分观测对象重叠。
```

配对准入规则：

```text
1. 恰好两个模态时，pairing_type 必须为 same_unit，两个模态的 obs_names 集合完全一致。
2. 三个及以上模态时，pairing_type 可以为 same_unit 或 partially_shared。
3. partially_shared 文件允许保留只属于一个模态的 observation，但至少一对模态必须有交集。
4. unpaired 不属于 schema 1.2 的合法取值；新数据应先在 temp/ 中整理出可配对内容。
```

---

## 9. 全集存储规范

全集是数据库中最基本的存储方式，必须完整满足本规范第 2 至第 8 节的通用要求。

全集的附加要求：

```text
1. dataset_type 必须为 full。
2. dataset_id 必须在数据库内唯一。
3. source 必须指向原始公开数据、实验数据或正式数据来源。
4. 不要求包含 source_dataset_id 和 source_obs_id。
5. 不要求包含 derivation；若存在，该字段必须为空，不得声明来源于其他训练集或测试集。
6. 全集应尽可能保存最接近原始测量的数据；但两模态来源必须先裁剪为共同 observation
   才能入库，未配对部分不进入 active catalogue。
```

全集可以包含多个样本、切片或独立空间坐标系，只要这些观测对象属于同一个逻辑数据集，并能通过 `sample_id` 区分。

---

## 10. 训练集和测试集存储规范

训练集和测试集必须首先满足全集所使用的通用矩阵、模态、观测信息和空间坐标规范，然后额外满足本节要求。

### 10.1 通用衍生要求

```text
1. dataset_type 必须分别为 train 或 test。
2. derivation 必须存在。
3. source_dataset_ids 只能包含全集 dataset_id。
4. 每个观测对象必须具有 source_dataset_id 和 source_obs_id。
5. 训练集或测试集中的每个来源二元组
   (source_dataset_id, source_obs_id)
   必须能够在对应全集中找到。
6. 不允许以训练集或测试集作为新的直接来源。
7. 任何特征筛选、特征对齐、数值转换或坐标变换都必须被记录。
8. challenge_type 必须存在并属于规范枚举，同一划分两侧必须一致。
```

### 10.2 单全集子集 `subset`

适用于训练集或测试集来自一个全集的情况。

要求：

```text
construction_type = subset
source_dataset_ids 的长度必须等于 1
所有 source_dataset_id 必须相同
每个 source_obs_id 必须存在于该全集中
```

子集可以基于以下对象进行选择：

```text
观测对象
样本
组织切片
空间区域
实验分组
其他可复现条件
```

具体选择规则必须写入 `selection_description`。若通过随机抽样产生，应填写 `random_seed`；若不是随机过程，可设为 `None`。

子集可以保留全部特征，也可以筛选特征，但必须记录 `feature_merge_policy` 和 `processing_description`。

### 10.3 多全集组合 `composite`

适用于训练集或测试集由多个全集共同组成的情况。

要求：

```text
construction_type = composite
source_dataset_ids 至少包含 2 个不同的全集 dataset_id
每个观测对象的 source_dataset_id 必须属于该列表
每个来源全集可以贡献全部或部分观测对象
```

多全集组合时必须处理以下问题：

```text
观测对象 ID 冲突：
    顶层 obs_names 必须全局唯一，并保留 source_obs_id。

样本 ID 冲突：
    必须对 sample_id 加来源全集前缀或采用其他可逆编码。

模态差异：
    衍生文件中声明的每个模态都必须具有明确的数据来源。
    不得用伪造的零矩阵代替来源全集中不存在的整个模态。

特征差异：
    同一模态的特征必须按稳定特征 ID 显式对齐。

坐标差异：
    不同样本的坐标可以保留在各自局部坐标系中，
    但 sample_id 必须能够区分这些坐标系。
```

### 10.4 特征合并策略

`feature_merge_policy` 推荐使用以下枚举：

```text
preserve:
    保留单一来源全集的原始特征空间，通常用于 subset。

intersection:
    仅保留所有相关来源全集共同具有的特征。

union:
    保留所有来源全集特征的并集。
    缺失特征必须使用能够明确表示“未测量”的方案，
    不得在没有依据时将其解释为真实零表达。

reference:
    对齐到指定全集的特征空间。
    此时必须额外提供 reference_dataset_id。
```

对于 `union`，如果矩阵格式无法区分“未测量”和“测量值为零”，不应直接采用普通零填充。应改用 `intersection`、`reference`，或增加能够明确表示缺失测量的辅助结构。

本项目的划分工具在 `union` 以及存在缺失目标特征的 `reference` 中采用以下辅助结构：

```python
adata.varm["feature_measured_by_source"]
```

该值是形状为“特征数 × 本衍生文件来源全集数”的布尔矩阵，列顺序由以下元数据明确记录：

```python
adata.uns["feature_measurement"] = {
    "mask_key": "feature_measured_by_source",
    "source_dataset_ids": ["full_a", "full_b"],
    "placeholder_value": 0,
    "description": (
        "False means the feature was not measured by that source "
        "dataset; the stored zero is not a true measured zero."
    ),
}
```

掩码中的 `True` 表示该来源实际测量了对应特征，`False` 表示未测量。X 中对应的零仅为存储占位，不能作为真实测量零解释。即使某个来源完全缺少该模态，也可以在掩码中保留一列全 `False` 以描述来源覆盖情况，但不得为该来源伪造该模态的观测行或零矩阵。

### 10.5 Challenge 分类

每个 Challenge 必须且只能声明以下三类之一：

```text
same_slice:
    训练集和测试集来自同一物理组织切片，在切片内部按空间区域或观测对象划分。

cross_slice_same_subject:
    训练集和测试集来自同一个体的不同物理组织切片，切片不得跨两侧重复。

cross_subject:
    训练集和测试集按个体边界隔离。该类包括同条件生物学重复，也包括不同条件、
    疾病状态或发育阶段的跨个体划分。
```

`challenge_type` 是对评估边界的显式声明，不能从 `construction_type` 推导：例如
`subset` 既可能表示单切片内部子集，也可能表示由一个完整个体构成的一侧。一次划分
不得混用不同层级的划分边界；分类依据及个体、切片分组规则必须在
`selection_description` 中说明。

### 10.6 训练集与测试集的对应关系

属于同一次数据划分的训练集和测试集必须使用相同的：

```text
split_id
challenge_type
```

同一个 `split_id` 下，训练集与测试集不得包含重复的来源观测对象：

```text
(source_dataset_id, source_obs_id)
```

同一个全集可以同时出现在训练集和测试集的 `source_dataset_ids` 中，但其贡献的来源观测对象必须互不重叠。

若划分单位是患者、样本、切片或其他高于单个观测对象的分组，应确保同一分组不会跨越训练集和测试集，并在 `selection_description` 中明确说明划分单位。

训练集与测试集不强制包含完全相同的来源全集，但应具有可比较的模态、特征空间和数值含义。任何差异都必须明确记录，避免模型评估时出现隐式不一致。

---

## 11. 最小验证函数

### 11.1 单文件验证

```python
def validate_minimal_h5mu(mdata):
    required_database_fields = {
        "schema_version",
        "dataset_id",
        "dataset_type",
        "source",
        "organism",
        "tissue",
        "spatial_unit",
        "coordinate_unit",
        "pairing_type",
    }

    valid_dataset_types = {"full", "train", "test"}
    valid_construction_types = {"subset", "composite"}
    valid_challenge_types = {
        "same_slice",
        "cross_slice_same_subject",
        "cross_subject",
    }
    valid_feature_policies = {
        "preserve",
        "intersection",
        "union",
        "reference",
    }

    # 至少两个组学模态
    assert len(mdata.mod) >= 2

    # 顶层观测对象
    assert mdata.n_obs > 0
    assert mdata.obs_names.is_unique
    assert "sample_id" in mdata.obs.columns
    assert mdata.obs["sample_id"].notna().all()

    # 空间坐标
    assert "spatial" in mdata.obsm
    spatial = mdata.obsm["spatial"]
    assert spatial.shape[0] == mdata.n_obs
    assert spatial.shape[1] in (2, 3)

    # 数据集级元数据
    assert "database" in mdata.uns
    database = mdata.uns["database"]
    assert required_database_fields.issubset(database.keys())
    assert database["schema_version"] == "1.2"
    assert database["pairing_type"] in {"same_unit", "partially_shared"}

    dataset_type = database["dataset_type"]
    assert dataset_type in valid_dataset_types

    global_obs_names = set(mdata.obs_names)

    # 各模态验证
    for modality, adata in mdata.mod.items():
        assert adata.X is not None
        assert adata.X.shape == (adata.n_obs, adata.n_vars)
        assert adata.n_obs > 0
        assert adata.n_vars > 0

        assert adata.obs_names.is_unique
        assert adata.var_names.is_unique
        assert set(adata.obs_names).issubset(global_obs_names)

        assert "assay" in adata.uns
        assert "technology" in adata.uns["assay"]
        assert "value_type" in adata.uns["assay"]

    memberships = [set(adata.obs_names) for adata in mdata.mod.values()]
    assert set.union(*memberships) == global_obs_names
    if len(memberships) == 2:
        assert memberships[0] == memberships[1]
        assert database["pairing_type"] == "same_unit"
    elif database["pairing_type"] == "same_unit":
        assert all(values == memberships[0] for values in memberships[1:])
    else:
        assert any(
            left.intersection(right)
            for index, left in enumerate(memberships)
            for right in memberships[index + 1:]
        )

    # 全集验证
    if dataset_type == "full":
        assert "derivation" not in database or not database["derivation"]

    # 训练集和测试集验证
    else:
        assert "derivation" in database
        derivation = database["derivation"]

        required_derivation_fields = {
            "construction_type",
            "source_dataset_ids",
            "split_id",
            "challenge_type",
            "selection_description",
            "feature_merge_policy",
            "processing_description",
        }
        assert required_derivation_fields.issubset(derivation.keys())

        construction_type = derivation["construction_type"]
        source_dataset_ids = derivation["source_dataset_ids"]
        feature_policy = derivation["feature_merge_policy"]

        assert construction_type in valid_construction_types
        assert derivation["challenge_type"] in valid_challenge_types
        assert feature_policy in valid_feature_policies

        assert isinstance(source_dataset_ids, (list, tuple))
        assert len(source_dataset_ids) > 0
        assert len(source_dataset_ids) == len(set(source_dataset_ids))
        assert all(
            isinstance(dataset_id, str) and dataset_id
            for dataset_id in source_dataset_ids
        )

        if construction_type == "subset":
            assert len(source_dataset_ids) == 1
        else:
            assert len(source_dataset_ids) >= 2

        if feature_policy == "reference":
            assert "reference_dataset_id" in derivation
            assert (
                derivation["reference_dataset_id"]
                in source_dataset_ids
            )

        assert isinstance(derivation["split_id"], str)
        assert derivation["split_id"]
        assert isinstance(
            derivation["selection_description"], str
        )
        assert derivation["selection_description"]
        assert isinstance(
            derivation["processing_description"], str
        )
        assert derivation["processing_description"]

        assert "source_dataset_id" in mdata.obs.columns
        assert "source_obs_id" in mdata.obs.columns
        assert mdata.obs["source_dataset_id"].notna().all()
        assert mdata.obs["source_obs_id"].notna().all()

        observed_source_ids = set(
            mdata.obs["source_dataset_id"].astype(str)
        )
        assert observed_source_ids.issubset(
            set(source_dataset_ids)
        )

        if construction_type == "subset":
            assert len(observed_source_ids) == 1

        source_pairs = mdata.obs[
            ["source_dataset_id", "source_obs_id"]
        ].astype(str)
        assert not source_pairs.duplicated().any()

    return True
```

### 11.2 训练集与测试集交叉验证

```python
def validate_train_test_pair(train_mdata, test_mdata):
    train_db = train_mdata.uns["database"]
    test_db = test_mdata.uns["database"]

    assert train_db["dataset_type"] == "train"
    assert test_db["dataset_type"] == "test"

    train_derivation = train_db["derivation"]
    test_derivation = test_db["derivation"]

    assert (
        train_derivation["split_id"]
        == test_derivation["split_id"]
    )
    assert (
        train_derivation["challenge_type"]
        == test_derivation["challenge_type"]
    )

    train_pairs = set(
        map(
            tuple,
            train_mdata.obs[
                ["source_dataset_id", "source_obs_id"]
            ].astype(str).to_numpy(),
        )
    )
    test_pairs = set(
        map(
            tuple,
            test_mdata.obs[
                ["source_dataset_id", "source_obs_id"]
            ].astype(str).to_numpy(),
        )
    )

    assert train_pairs.isdisjoint(test_pairs)

    return True
```

上述函数验证文件内部结构、训练集和测试集的 `split_id` 与 `challenge_type` 一致性，以及
两侧来源观测对象是否重叠。要完整确认衍生关系，还应加载对应全集，检查每个：

```text
(source_dataset_id, source_obs_id)
```

是否真实存在于来源全集中，并检查来源数据集的 `dataset_type` 是否为 `full`。

---

## 12. 不强制保存的内容

以下内容全部为可选，不应影响文件是否通过最小验证：

```text
layers
raw
PCA
UMAP
聚类结果
细胞类型注释
QC 指标
归一化矩阵
高变基因
空间邻接图
组织图像
细胞分割边界
细胞核边界
转录本级坐标
配体受体分析结果
完整分析流程
```

以下衍生信息也是可选的，但在条件允许时建议保存：

```text
数据划分脚本版本
代码仓库提交 ID
随机抽样算法
数据过滤阈值
患者或样本分组字段
创建时间
创建者
```

---

## 13. 最小必需信息总结

一个合格的最小 `.h5mu` 文件必须能够回答：

```text
1. 有哪些组学模态？
2. 每个模态的数据矩阵是什么？
3. 每一行对应哪个空间观测对象？
4. 每一列对应哪个生物学特征？
5. 每个观测对象属于哪个样本？
6. 每个观测对象的空间坐标是什么？
7. 矩阵中的数值代表什么？
8. 数据由什么技术产生？
9. 数据来自哪里？
10. 空间单位和坐标单位是什么？
11. 不同模态是否测量同一批观测对象？
12. 当前文件是全集、训练集还是测试集？
```

对于训练集和测试集，还必须能够回答：

```text
13. 它由哪些全集衍生而来？
14. 它是单全集子集还是多全集组合？
15. 每个观测对象在来源全集中的原始 ID 是什么？
16. 使用了什么数据选择规则？
17. 多全集之间如何对齐特征？
18. 数据矩阵和空间坐标经历了哪些处理？
19. 它属于哪一次训练/测试划分？
20. 该划分属于同切片、同个体跨切片还是跨个体 Challenge？
21. 训练集和测试集之间是否存在来源观测对象重叠？
```

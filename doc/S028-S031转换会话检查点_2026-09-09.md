# S028–S031 转换会话检查点（2026-09-09）

用户要求完成已启动任务、保存结果与会话进度，不再启动新的耗时任务，然后结束回合。
本次只检查已有文件、进程和报告并保存检查点；未启动转换、重放、全量验收或入库。
只读进程检查没有发现遗留转换进程，当前没有存活的子 agent。

## 保存状态

| 条目 | 首次产物 | 已有验证证据 | 重放状态 |
| --- | --- | --- | --- |
| S028 | 两份 H5MU 已保存，2133 obs | 首次 schema/独立源值/过滤统计通过报告已保存 | **中断，未完成**；没有重放 H5MU，保留 ATAC 中间 NPZ 和空日志，不宣称重放通过 |
| S029 | 两份 H5MU 已保存，2221 obs | 修正蛋白来源 feature ID 后的 v2 验证通过 | 两份重放、字节哈希一致及全 HDF5 比较通过报告已保存 |
| S030 | 两份 H5MU 已保存，2500 obs | YAML publication 格式修正后的独立验收通过 | 两份重放、字节哈希一致及全 HDF5 比较通过报告已保存 |
| S031 | 两份 H5MU 已保存，9686 obs | schema/独立源值/过滤统计通过 | 两份重放、字节哈希一致及全 HDF5 比较通过报告已保存 |

以上 PASS 引用此前实际运行的报告。本次未重新哈希多 GB 数据文件或重新运行验证，
不能将本次检查点视为新的数据完整性验收。

四条目前均未入库；只读目录检查仍为 187 full、29 train、29 test。
原始数据、失败候选、现有产物、代码快照、环境锁、映射、参考及审计结果均保留。
没有删除文件、更新 intake、重启服务或提交 Git。

## 恢复入口

- 会话状态：`temp/mux_execution_20260907/session_checkpoint/progress.json`
- 本次文件清单：同目录 `artifact_inventory_20260909.json`（路径/大小/时间；不是重新计算的内容哈希）
- 共享验收代码和报告：`temp/mux_execution_20260907/`
- 每条复现材料：`temp/S0xx_spatial-mux-seq/.work/import_20260907/reproducibility/`
- 首次产物：`temp/S0xx_spatial-mux-seq/processed/<dataset_id>/`
- 每条中断/恢复记录：对应工作目录 `session_checkpoint_20260909.json`

## 待用户重新授权继续执行的事项

1. S028 需要在新隔离目录重新完成整条重放，不能把现有 ATAC 缓存当成完整重放。
   在执行前检查 recipe 与实际代码/产物版本，特别是 provenance 补写步骤是否确实执行。
   随后补齐产物旁的正式 validation_report、checksum、conversion_audit 和最终复现清单。
2. S030 仍需确认复现包收尾完整；S031 的 `commands.tsv` 仍含解包占位叙述，
   `code/entry/replay_s031.sh` 复制后相对路径不直接成立，需补可移植入口并记录验证范围。
   未完成的入口修复不得标作已验证。
3. 核对全部八份当前文件与已有报告所记哈希一致，补完主 agent 最终验收，之后才串行入库。
4. `accept_and_import.py`、`update_intake_after_import.py` 仅为预备脚本，**从未执行**。
   前者目前对 `replay_report.json` 和 `commands.sh` 的文件名假设过窄；已有合法证据也使用
   `replay_report_v2.json`、`reports/replay_comparison.json`、`commands.json` 等名称。
   恢复时应核对内容并修正证据定位，不能改名伪造缺失证据或直接跳过验收。
5. 入库后的四处 entry_id/checksum 核对、intake 更新、服务重启和页面检查均未执行。

科学决定保持已批准版本：组织内各模态 obs 交集、same_unit；固定 5 kb 双端计数，
主矩阵不乘 multiplicity，另存 read_support；固定 mm10 blacklist 过滤后的 493982 特征；
S031 保留 121 生物靶标和 9 个标记清楚的对照抗体。S038、S044、S062 不在本次执行范围。

# 空间信号缩略图发布记录（2026-09-10）

本轮为缺图的 **39 个 Database** 发布空间预览，现有预览覆盖由 **192/231** 提升为
**231/231**。原有 192 张图片保持原字节不变。

| Entry | RNA 信号图 | 空间点密度图 |
| --- | ---: | ---: |
| C002 | 1 | 1 |
| GSE213264 | 1 | 0 |
| S007 | 3 | 0 |
| S033 | 2 | 0 |
| S038 | 11 | 0 |
| S047 | 1 | 0 |
| S051 | 0 | 11 |
| S065 | 8 | 0 |
| **合计** | **27** | **12** |

使用 `spatial-signal-v1` 方法，固定参数见[空间信号缩略图](空间信号缩略图.md)。
S065 显示时恢复交错 Visium 网格比例，低 RNA 信号连续淡入白底；S051 明确标记
`Local field`。全部图片保留采样范围和内部空白，未推断二值组织掩膜。
所有输出为最大边长 640 px 的 RGB WebP，quality 85、method 6。

## 重放与保护检查

忽略目录 `temp/spatial_thumbnail_20260910/` 保留输入身份、实际 SHA-256、代码快照、
Conda explicit spec、pip 版本、逐文件 recipe、逐观测 NPZ、四张联系表、命令和验证脚本。

冻结代码分别在干净的 `approved/` 与 `replay/` 目录运行，39 个文件的有序 observation
和 feature IDs、原始／显示坐标、信号和缩放信号数组、解码 RGB 像素、WebP 字节、JSON
说明全部相等。正式候选图还与逐张视觉检查过的首轮图片像素完全一致。
上游作者采集和既有 catalogue 转换不属于本次重放，正式 H5MU 是本流程的输入边界。

发布时复制已验收文件的原始字节，通过拒绝覆盖的原子链接发布，并由启动加载器重新校验
全部 39 个说明文件。原有图片、catalog.db 和难度快照共 **194 个受保护文件**的哈希均
保持不变；正式 H5MU 没有写入，无须刷新 Challenge 难度快照。

## 页面与测试

- 定向测试首轮完整相关集合 **46 项通过**；随后增加尺寸异常用例并针对启动校验重跑
  **7 项通过**，共覆盖 47 个不同用例。Ruff 和 `git diff --check` 通过。
- 重启后的读取验证覆盖 **231 个文件详情页、40 个 entry 页、14 个列表页和39 张新图**。
- 231 个 Database 和 29 个 Challenge 的公开列表 JSON 与发布前完全相同。
- Chromium 在 1440 px 和 390 px 下检查列表、entry、RNA 和密度详情，共 **14 次页面
  检查通过**；没有图片解码失败、横向溢出、截断标识、过小标识或控制台错误。
- 根据浏览器检查，将空间预览条的背景设为白色，列表空间图高度限制为 128 px，并让
  详情中的长元数据文本换行，修复 S065 来源说明引起的横向溢出。原生图片显示规则保持原样。

沙箱内 ASGI 检查曾卡在 asyncio/AnyIO 线程唤醒，停止该进程后，在允许本地 IPC 的环境中
通过定向测试。首次发布重启因会话中断未完成，恢复后完成重启和线上检查。
没有运行完整 pytest 套件。

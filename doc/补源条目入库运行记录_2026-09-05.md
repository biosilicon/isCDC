# 补源条目入库运行记录（2026-09-05）

> 后续状态：同日已按空间观测层级排除区域级条目，S020/S022 的31份full下架归档，当前187个Database、29项Challenge。本文补源/入库数量保留为当时记录；最新资格与暂停清单见[空间观测层级审计](空间观测层级收录审计_2026-09-05.md)。

本轮承接 [16 条目补源复核](16条目补源与来源身份修复记录_2026-09-05.md)，按用户“继续执行，完成入库”
指令执行。用户随后明确将 **S049 移出数据库收录范围**，已取消其转换和入库计划；确认目录没有
S049 记录，原始资料和补源审计保留。

## 执行范围

| Entry | 计划新增 full | 固定处理方式 |
|---|---:|---|
| C008 | 1 | 作者 MAGPIE 大鼠肺共同单位；3,971 spots，GEO raw RNA，339 MSI peaks；明确相邻切片配准/聚合 |
| S012 | 1 | 5,756 spots；18,085 RNA、35 protein；raw protein UMI 与 isotype-normalized layer |
| S014 | 1 | AddOns 独立样本，4,908 spots；18,085 RNA、41 protein；保留全部通道 |
| S027 | 5 | 每片完整 2,500 点 raw grid；RNA/ATAC counts 原值与官方 barcode 坐标；不把 is_cell 当组织掩码 |
| S033 | 2 | 原始 RNA counts；5 kb ATAC endpoint-only binary tiles；不填充人工片段跨度 |
| S045 | 9 | 作者 23,269 个明确一对一配对；GEO RNA 与原始 MSI CSV；DHB 为 lipid，其余为 metabolite |
| S046 | 4 | 四个作者配对对象共 15,060 spots，原始 RNA 与唯一完整谱匹配 MSI；保留配准来源 |
| S051 | 11 | 逐 FOV 发布 FCS，31,156 cells；排除 DNA1/DNA2，24 protein 与 189 代谢通道原值 |
| **合计** | **34** | 全部为 full；不新建或替换 train/test |

本轮没有计划内公开辅助文件；来源图像只用于内部坐标/配准检查。所有条目省略 canonical
`cell_type`，因为没有经核验的来源离散标签。新增受控 technology：`SPACE-seq`、
`Spatial Multimodal Analysis`、`scSpaMet`。S051 数值按发布文件保留，归一化阶段未明确时
使用 `value_type: unknown` 并记录原因；不声称为 raw counts，也不捏造未声明的许可。

八份最终计划存于各 `temp/<entry>/final_plan_20260905.md`。源文件与输出隔离，worker 只写各自
工作区并验证，主 agent 独立复核后串行调用正式 importer。S027 既有三个 S1 样本不属于本轮
新增目标。所有原始文件、blob 与 staging 保留。

## 验证与结果

<!-- results:start -->
**已完成：8 个条目新增 34 个 full，34/34 正式导入与发布检查通过，失败 0。**

本轮入库完成时，本地 catalogue 共 **276 个数据文件：218 full、29 train、29 test，37 个 entry ID**。
原有 242 条 catalogue 记录逐字段保持一致；S027 原有三个 full 未被替换，当前该 entry 共八个 full。
S049 catalogue 记录数为 0，来源 manifest 中保留 `excluded_by_user` 决定。

| Entry | 新增 full | Observations | 正式 validator 警告 |
|---|---:|---:|---:|
| C008 | 1 | 3,971 | 0 |
| S012 | 1 | 5,756 | 0 |
| S014 | 1 | 4,908 | 0 |
| S027 | 5 | 12,500 | 0 |
| S033 | 2 | 6,825 | 0 |
| S045 | 9 | 23,269 | 0 |
| S046 | 4 | 15,060 | 0 |
| S051 | 11 | 31,156 | 22 |

新增 H5MU 合计 3,418,587,392 bytes。主控逐一重新打开输出，独立回查原始矩阵、
feature/barcode 与空间坐标，并验证 schema、SHA-256 和输出身份。正式 importer 串行运行后，
每份均再次核对 H5MU、YAML、manifest、catalogue 四处 `entry_id`，以及正式文件实际大小和 SHA-256。

S033 worker 双向追溯全部 63,607,360 条片段记录；主控另外独立扫描全部片段、各选 17 个
观测行重建端点集合，并核对 RNA 全矩阵、官方坐标、完整 tile 轴与 blacklist 清零，均一致。
端点使用作者/ArchR 的一基坐标 `floor(endpoint / 5000)`；保留 canonical autosomes 与 chrX，
排除 chrM/chrY/非 canonical 染色体，blacklist tile 保留在特征轴但值为零。额外补存并登记
UCSC mm10/hg38 chromosome sizes 和固定 ArchR commit `6feec354ad6c8052ddbc4626a2ca2d858ed465bf`
的两份 genome annotation，四份输入均通过 SHA-256 校验；未记录的 RDA 精确下载时间未补造。

S051 的 22 个警告均为两模态 `unknown_value_type`，对应发布 FCS 未说明精确归一化阶段；
保留 float32 原值和 189 个发布代谢通道。每份正式 Source 同时记录 Zenodo DOI 与该 FOV 的
GitHub 固定 commit `1cd3d21c35b6da6504037f2c2bca2a420eca931e` 文件 URL。S045 各配对子集中
有 12,352–14,254 个全零 RNA features，按计划保留完整 32,285 特征轴；这是转换报告说明，
正式 schema validator 为 0 警告。S045/S046 来源声明的 CC BY-NC 3.0 已如实记录在报告中。

Challenge 集合及 58 个 train/test 记录与哈希未变，本批无需重算 difficulty。快照 SHA-256 仍为
`12cd0e40070bd7214ffff05012a81b52352de999d128403ad9a7a31d06f1d275`。统一重启应用后，健康检查、34 个新增 Database 的详情页与 JSON、
8 个 entry 分组、S049 的 404、全部 29 个 Challenge 指标和两种难度排序均通过直接 HTTP 检查。

受控 technology 的针对性 schema/validation 测试 **31 passed**，Ruff 和 `git diff --check` 通过；
纯数据导入未运行全套测试。详细 acceptance/import、基线备份、来源审计及发布记录位于
`temp/import_recovered_20260905/`；worker 产物和原始数据均保留，没有删除 temp 或源 blob。
<!-- results:end -->

## 未进入本批的条目

- **S049**：用户明确排除；不再列为待入库。原始 DESI 35 µm、蛋白采样 ROI 200×200 µm，
  配对数据的共同观测单位为 200 µm 尺度区域。
- **S008**：同一固定官方 RNA GEF 的新一轮小范围重试仍因 TLS 握手失败，尚未恢复必要来源。
- **S036**：精确 Dryad archive 的 API/公开下载入口仍存在访问限制；本轮没有取得等价完整来源。
- **S050、S052、S053、S054**：仍缺对应 FOV 的完整作者配准/最终 mask/单细胞配对产物。
- **S064**：仍缺其实际 barcode 的空间映射。

上述缺口不能用单模态文件、猜测配准/坐标、像素替代或重分割来补造符合规范的 paired 数据。
来源记录中的历史补源成功，不代表这些条目自动具备入库条件。

# 空间域已完成结果发布（2026-09-20）

用户授权先发布已完成结果并执行发布脚本。本次执行：

```bash
bash annotation/spatial_domain/publish_completed.sh --completed-only
```

2026-09-20 01:32:15 UTC 发布成功，正式目录为 `data/spatial_domain_visualizations/`。
共发布 216 个 Database、216 个样本，其中 BANKSY 70 个、GraphST 146 个。全部源 H5MU
摘要、sidecar、暂存结果、详情页 generation 和样本点位文件均校验通过。网站由
`deploy_test.sh` 停止并重新启动，健康检查及前端 bundle 校验通过。

本次在 01:28:37 UTC 固定成功清单，进度快照时间为 01:28:13 UTC。
`xenium_human_ccrcc_ffpe_rna_protein` 当时仍在计算，记入 `deferred`；14 个输入资格不满足
的 Database 保留为 `skipped`（12 个无 RNA、2 个预处理依据未核实）。没有将这些任务记作
发布成功，也没有写入 catalogue 或改写源 H5MU。

计算 supervisor `1026348` 未停止。发布后 01:32:16 UTC 心跳仍为 `running`，唯一活动
任务 PID 为 `1099297`，与发布前一致。后续完成后再次执行同一命令即可补充发布。

运行证据：

- 计算目录：`temp/spatial_domain_repaired_20260919T141044Z/`。
- 发布报告：`temp/spatial_domain_publication_20260920T012837Z-9db168a9/`，含 `audit.json`、
  `events.jsonl`、`service.log`、`published.json`。
- 完整终端输出：`temp/spatial_domain_partial_publish_20260920.log`。
- 正式 release：
  `data/.spatial_domain_visualizations-releases/spatial_domain_publication_20260920T012837Z-9db168a9/`，
  其中 `transaction.json` 状态为 `published`；正式目录指向该 release 的 `sidecars/`。

发布脚本新增显式 `--completed-only` 选项；默认完整发布模式仍保留。部分发布使用独立的
目标目录发布锁和已选 generation 锁，允许 supervisor 继续计算。开始后新完成的任务留待
下次调用；未完成或失败任务的现有正式结果保留。成功结果仍执行全部来源和内容校验，
启动或 HTTP 验证失败仍回滚。

相关测试 48 项通过，覆盖运行中的 supervisor/资源锁、固定成功清单、各类暂缓状态、
旧结果保留、选定 generation 防并发改写及原有发布/回滚行为。Ruff、Shell 语法和差异空白
检查通过。使用说明见[先发布已完成结果](空间域识别.md#先发布已完成结果)。

## 同日：可视化切换入口修复

保留同一区域、同一个 canvas，将不易发现的 `Color by` 下拉框改为始终可见的
`Cell types` / `Spatial domains` 按钮，选中态同步高亮及 `aria-pressed`。默认选择有效的
细胞类型注释；缺失或无效结果的按钮禁用，并提示该类结果不可用。保留原有样本、相机、
独立图例状态及方法说明，不修改科学结果、源文件或 catalogue。

现有细胞类型 sidecar 的只读审计确认 32 份有效，另有 4 份源 SHA-256 已过期：
`2023_nc_10x_breast_cancer_HBC_rep1`、`visium_human_tonsil_rna_protein`、
`xenium_human_ccrcc_ffpe_rna_protein`、`xenium_human_rcc_ffpe_rna_protein`。
这 4 份已在 `temp/spatial_resolution_20260910/migration_report.json` 的
`preexisting_stale_visualizations` 中记录；本次不绕过来源校验，也不将它们描述成可用结果。

验证包含空间域相关 Python 测试（18 项，修正测试解析器后仅重跑受影响的 2 项通过）、
4 个前端测试文件、真实数据 Chromium 切换测试、桌面/手机截图与布局检查。
截图、侧文件审计及浏览器证据保存于 `temp/spatial_window_fix_20260920/`；两种视图反复
切换后无控制台错误或水平溢出，缺少细胞类型结果时禁用状态与提示正常。

网站重启成功后，逐页验证全部 32 份有效细胞类型结果仍为默认视图，每页只有一个画布，
线上 bundle 与本地摘要一致。线上细胞类型交互与空间域切换两项 Chromium 测试均通过；
核对记录见 `temp/spatial_window_fix_20260920/live-verification.json`。

## 最终补发 ccRCC（06:20 UTC）

全量队列于 2026-09-20 06:07:34 UTC 完成：217 项成功（BANKSY 71、GraphST 146），
14 项因输入资格跳过，无失败、等待或运行中的任务。按用户授权执行
`bash annotation/spatial_domain/publish_completed.sh`，于 06:20:25 UTC 完成完整发布。

实际新增仅 `xenium_human_ccrcc_ffpe_rna_protein`，generation 为
`20260920T060719-ac0b68690981`。原有 216 份 generation 全部保持不变，无移除项。
ccRCC 共 690,322 个观测，其中 685,580 个完成 BANKSY 分析，分为 21 个空间域；
另 4,742 个标为 `Not analyzed`，不将该类别计为一个空间域。

发布脚本重新验证源摘要和 sidecar、原子切换目录并重启网站，全部 217 个空间域详情页及
点文件的 MIME、长度、SHA-256 校验通过。另行核对全部 88 份细胞类型页面，原 generation
与默认细胞类型选择均保留，前端 bundle 与本地一致。

补发后的 ccRCC 页面已通过桌面（1440×1100）及手机（390×844）Chromium 实测：
同一画布可在细胞类型与空间域之间往返切换，加载的 generation 与本次发布一致，
细胞类型视图恢复正常，`Unannotated` 默认隐藏，无浏览器错误或水平溢出。
两种尺寸的空间域截图均已人工查看。

本次发布证据：

- `temp/spatial_domain_publication_20260920T061538Z-2dfd25dc/`：审计、日志和成功凭据。
- `data/.spatial_domain_visualizations-releases/spatial_domain_publication_20260920T061538Z-2dfd25dc/`：
  新 release，事务状态 `published`；正式目录指向其 `sidecars/`。
- `temp/spatial_domain_final_20260920/publication_delta.json`：实际仅新增 1 项。
- `temp/spatial_domain_final_20260920/cell-type-preservation.json`：88 份细胞类型保留核验。
- `temp/spatial_domain_final_20260920/browser-results.json`：桌面及手机切换核验；
  同目录 `desktop-domains.png`、`mobile-domains.png` 为截图。

## SpatialGLUE 成功组合上线（17:02 UTC）

按用户“先上线其他的”“立即执行上线操作”的授权，于 2026-09-20 17:02:16 UTC 完成
SpatialGLUE 部分发布。计算批次共 208 个组合，207 个成功、1 个失败；发布覆盖 201 个
Database、207 个组合。两个四模态数据集各发布全部四个三模态组合：
`GSE263333_mouse_brain_5m_20um_spatial_mux_h3k27ac` 和
`GSE263333_mouse_brain_5m_20um_spatial_mux_h3k27me3`。

执行命令：

```bash
bash annotation/spatial_domain/publish_completed.sh \
  --method spatialglue --run-root temp/spatialglue_full_nvml_20260920 --completed-only
```

成功清单于 16:53:31 UTC 固定；全部冻结文件、当前 catalogue 绑定、源 H5MU 摘要、
计算适配器、环境锁、完整训练参数和 sidecar 校验通过。17:00:15 UTC 切换正式指针，
随后网站启动成功。207 个组合的线上页面 generation、样本和点位文件 MIME、长度、
SHA-256 均通过核验，前端 bundle 与本地一致。旧 release 保留，源 H5MU 和 catalogue
未改写；原有 RNA 结果随旧目录一并保留。
上线后逐页核对原有 217 个 RNA 页面，217 份 RNA 结果和这些页面中的 85 份细胞类型
结果 generation 均保持不变，默认视图也保持一致；凭据为发布报告目录中的
`preserved_views.json`。

唯一暂缓项为 `GSE270498_spatial_dmt_me11_replicate_50um` 的 `rna__methylation`。
旧批次错误地将该来源的 signed MethSCAn residual 当成 `[0,1]` 甲基化比例，导致失败。
本次仅发布其他成功项，该数据的修复代码和补算独立处理，不将失败状态或未完成结果发布。
另有 29 个微生物相关 Database 按准入规则排除，不计入成功数。

本次发布证据：

- `temp/spatial_domain_publication_20260920T165330Z-9a0ed440/`：`audit.json`、`events.jsonl`、
  `service.log` 和 `published.json`；后者确认 207 个组合发布成功。
- `temp/spatialglue_publish_completed_20260920.log`：完整发布日志。
- `data/.spatial_domain_visualizations-releases/spatial_domain_publication_20260920T165330Z-9a0ed440/`：
  新 release，事务状态为 `published`，正式目录已指向其 `sidecars/`。
- `temp/spatialglue_publication_previous_views_20260920.json`：切换前原有页面视图快照。

发布入口已补齐 SpatialGLUE 的组合路径、冻结队列审计、组合 generation 锁与线上接口校验，
沿用现有事务切换和回滚逻辑。发布相关 39 项用例通过，新增覆盖失败组合暂缓、其他组合
及 RNA 结果保留、源文件变化拒绝发布；未运行完整测试套件或增加资源测试。

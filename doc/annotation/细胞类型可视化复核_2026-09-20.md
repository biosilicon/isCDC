# 细胞类型可视化复核（2026-09-20）

以下为接入前的审计快照。后续已按用户授权恢复 4 份历史结果、发布 52 份来源标签，
当前共 88 份可视化上线；见[接入记录](细胞类型可视化接入记录_2026-09-20.md)。

本次为只读复核，没有重新运行注释算法、修改正式 sidecar、写入源 H5MU/catalogue 或重启网站。
复核覆盖当前全部 231 个 `full` Database，不把 2026-08-18 的历史全量审计视为当前覆盖率。

## 当前覆盖情况

| 状态 | 数量 | 结论 |
| --- | ---: | --- |
| 有效且网站可见 | 32 | sidecar 完整性及当前来源绑定均通过 |
| 原有结果来源绑定过期 | 4 | 结果未损坏；已确认仅元数据变化，可以重新绑定后发布 |
| 无 sidecar，但源文件已有 `obs.cell_type` | 52 | 标签结构检查通过，尚未配置和生成来源标签可视化 |
| 无 sidecar，也无顶层 `obs.cell_type` | 143 | 不在现有注释计划中，需另行检查输入资格、参考库和方法 |
| 合计 | 231 | 现有注释计划仅包含 36 个数据集 |

全部 231 个线上页面均与 sidecar 审计一致，没有发现有效细胞类型结果被空间域视图遮蔽或遗漏。
32 份页面的 generation 与当前状态文件一致。缺少细胞类型结果时仅显示可用的空间域结果，
不能据此判断细胞类型注释曾经存在。

## 4 份旧结果为何不可见

| 数据集 | 既有方法 | 观测数 |
| --- | --- | ---: |
| `2023_nc_10x_breast_cancer_HBC_rep1` | 来源标签 | 167,780 |
| `visium_human_tonsil_rna_protein` | RCTD | 4,194 |
| `xenium_human_rcc_ffpe_rna_protein` | SingleR | 465,534 |
| `xenium_human_ccrcc_ffpe_rna_protein` | 来源标签 | 690,322 |

4 份 sidecar 的 manifest、report、点文件及压缩表示在原来源绑定下全部通过校验。
`data/backups/technology_vocabulary_20260830T104844Z/datasets/` 中对应 H5MU 的完整 SHA-256
分别与这 4 份 sidecar 记录的原 SHA-256 精确相同；当前 H5MU 完整 SHA-256 与 catalogue 一致。

逐一比较旧备份和当前 HDF5 的全部对象、属性及数据值，变化仅为：

- RNA/蛋白模态的 `uns/assay/technology` 名称规范化；
- 新增顶层 `uns/database/entry_id` 与 `original_spatial_unit`；
- `uns/database/spatial_unit` 从旧类别名变为 `single_cell` 或 `spot_level`。

RNA/蛋白矩阵、基因、观测顺序、样本、坐标及既有来源标签均未变化。另行重算当前 observation
顺序摘要、逐点核对 float32 坐标，4 份全部一致；两份来源标签与点文件标签逐项一致。

8 月 30 日技术名称迁移更改了 H5MU 文件摘要，旧注释来源绑定没有同步更新。9 月 10 日的
分辨率迁移报告已将这 4 项记录为 `preexisting_stale_visualizations`，故不属于空间域计算
覆盖注释，也不是 SingleR/RCTD 的科学质量失败。

可以在保留旧 generation 的前提下，生成绑定当前源 SHA-256 的新 generation，更新
manifest/report/status 的相应摘要并完整验证后发布；不需要重算 SingleR/RCTD，也不应
取消网站的来源校验。本次仅完成复核，尚未执行该恢复。

## 52 份有来源标签、尚无可视化的结果

- 39 份 IN-DEPTH CosMx；
- 11 份 GSE328481 Stereo-XCR；
- 2 份 zenodo8041114 RIBOmap/STARmap。

共 1,397,951 个观测。52 份全部通过无序 categorical、观测覆盖、非空/无首尾空格标签、
无未使用类别的结构检查；含 `Unannotated` 的项目具有来源 provenance 字段。
该检查不替代对 provenance 内容的完整核验。

这些数据集不在 `assets/cell_type_annotation/configs/catalogue.yaml` 中，网站也不会从源 H5MU
即时生成点文件。下一步应复用来源标签，补齐来源证据和 source 方法配置，再生成并验证
可视化 sidecar；不需要仅为显示既有标签而运行推断算法。余下 143 份需单独制定注释方案，
不能直接套用历史参考库或保证全部具备推断资格。

## 证据

复核产物保存在 `temp/cell_type_recheck_20260920/`：

- `audit.json`：231 项状态、历史配置覆盖和旧绑定校验；
- `compare_sources.py`、`source_comparison.json`、`comparison.log`：4 项完整文件摘要与 HDF5 比较；
- `alignment.json`：观测顺序、坐标及来源标签一致性；
- `source_label_structure.json`：52 项来源标签结构检查；
- `live_pages.json`：231 项线上页面对照，无不一致项。

注释数据核验使用隔离的 `iscdc-cell-annotation` 环境；HTTP 页面核对使用网站 `iscdc` 环境。

# 细胞类型可视化接入记录（2026-09-20）

按用户“接入目前能接入的所有细胞类型可视化，包括历史结果”的要求，在原有 32 份有效结果
基础上，补建 52 份来源标签可视化并恢复 4 份历史结果。本次没有重新运行 SingleR/RCTD，
不更改源 H5MU、catalogue、既有空间域 generation 或标签语义。

## 范围与来源证据

| 接入类别 | 数量 | 依据 |
| --- | ---: | --- |
| IN-DEPTH CODEX–CosMx 来源标签 | 39 | S023 精确三键连接后保留作者 `cell_type` |
| GSE328481 Stereo-XCR 来源标签 | 11 | S038 保留作者 `anno2`；P5 的 27 个来源未匹配观测为 `Unannotated` |
| zenodo8041114 RIBOmap/STARmap 来源标签 | 2 | C002 保留作者 `level_3`，逐观测核对 RNA/translatome 与配准蛋白 |
| 元数据变化后重新绑定历史结果 | 4 | 旧来源完整 SHA-256 与备份相符，科学输入、标签和坐标未变 |

来源标签核验记录：`temp/S023_in-depth/worker_report.md`、
`temp/S038_stereo-xcr/worker_report.md`、`temp/C002_ribomap-starmap/worker_report.md`。
52 份来源标签在接入前合计 1,397,951 个观测，全部通过项目 `_validate_cell_type` 校验，包括
部分覆盖的完整 provenance 契约。新增配置显式使用 `method: source`、无需校准，不配置
reference，也不生成置信度。

4 份历史结果为乳腺 Xenium 来源标签、扁桃体 Visium RCTD、肾癌 Xenium SingleR、ccRCC
Xenium 来源标签；见[来源比较与复核](细胞类型可视化复核_2026-09-20.md)。新的 immutable
generation 仅更新当前来源绑定及关联摘要，原点文件、置信度、`inference.h5` 和压缩表示
逐字节保持不变，原 generation 保留。

## 历史覆盖核对

补查历史配置、正式目录、备份、隐藏 staging、`temp/`、`exp/`、`/tmp`、用户临时目录及旧
`in_silico_central_dogma_challenge` 项目的可读候选文件，共检查 3,888 个候选路径。
103 份成功 generation 对应 36 个实际数据集；同一数据集所有历史 generation 的点文件
摘要一致。90 份失败报告为旧轮次记录，21 个遗留 work 目录仅有输入/运行配置，没有额外
完成结果。正式目录外匹配到的两个 cell-type manifest 均为 `test_rna_protein` 测试夹具。
原 35 项注释计划全部保留，后增 ccRCC 共 36 项，无丢失的成功数据集。

还检查了未接入文件的顶层及模态内 annotation/label/cluster 字段：5 份为来源 Seurat
聚类编号，11 份 scSpaMet `source_cell_label` 为逐细胞编号，均不作为生物细胞类型导入。
余下 143 个 full 数据集没有可直接接入的细胞类型结果，需要另行核验公开标签或制定推断
方法与 reference；本次不进行没有依据的全库重新推断。

## 生成、兼容性与验证

- 所有生成/历史恢复在隔离 `iscdc-cell-annotation` 环境执行，52 个来源任务使用 6 个并发
  进程、每进程 BLAS/OMP 1 线程，在隔离目录写产物，逐任务日志记录状态和 ETA。
- 该环境 AnnData 0.11.4 读取新文件中的 HDF5 `null/0.1.0` 时需要兼容 reader。来源标签
  读取复用了空间域流程已有的惰性 backport；没有升级锁定环境或改写原文件。
- 56 份待接入结果合计 2,725,781 个观测。校验完整源文件 SHA-256、observation 顺序摘要、
  每样本逐点坐标、来源标签逐点一致性、sidecar 完整性与压缩点位表示；全部通过。
- 代码验证包括有/无 null 元数据的来源标签读取、严格 sidecar 发布、88 项计划配置、惰性
  import，共 5 个 pytest 用例；Ruff 与 whitespace 检查通过。

## 发布与恢复

运行目录：`temp/cell_type_integration_20260920/`。

- `prepare.py`、`prepare.log`、`events.jsonl`、`logs/`：来源标签生成及进度。
- `canonical_label_validation.json`：52 项 canonical 标签/provenance 校验。
- `recover_history.py`、`history_recovery.json`：4 项历史绑定恢复及保留文件清单。
- `verify_stage.py`、`stage_verification.json`：56 项逐点复核和来源绑定。
- `publish.py`：串行发布；预先复制不可变 generation、备份全部旧状态文件，检测并发状态
  变化，停止服务后切换状态，重启后检查全部页面和点位。失败时恢复旧状态并重启。
- `previous-status/`、`transaction.json`、`publication.jsonl`、`service.log`：状态备份和事务。
- `published.json`：仅全部线上检查成功后生成，作为发布完成凭据。

原目录的 references、旧 generation 和工作记录均保留。既有后台空间域计算继续独立运行。


## 发布结果

2026-09-20 02:46:40 UTC 发布完成，网站健康检查通过。当前 88 份细胞类型可视化均已上线：
56 source、31 RCTD、1 SingleR。逐页检查全部 231 个 Database，核对全部 88 份细胞类型
点文件的 MIME、长度和 SHA-256，均通过；每页默认选中有效细胞类型，既有空间域 generation
全部保持不变。事务状态为 `published`，完成凭据为上述 `published.json`。

真实浏览器抽查 CosMx、Stereo-XCR P5、历史 RCC 与 ccRCC 均通过；覆盖桌面和手机布局、
source/SingleR 方法说明、可用空间域切换以及 P5/ccRCC 的 `Unannotated` 初始未选中。
4 个页面均无控制台错误和水平溢出。浏览器记录为 `browser-results.json`，截图为同目录下
`cosmx.png`、`stereo-p5.png`、`rcc.png`、`ccrcc.png`。
